{
  "skill":"agy-supervised-development",
  "version":"4.1.0-alpha.3",
  "execution_status":"SCENARIOS_DEFINED_NOT_EXECUTED",
  "cases":[
    {"id":"contract-content-bound","expected":"same revision changed Contract body is rejected"},
    {"id":"runner-receipt-source-of-truth","expected":"runner executes frozen argv and immutable receipt is authoritative"},
    {"id":"structured-metric-reparse","expected":"completion reparses hashed report and rejects contradictory measured values"},
    {"id":"business-na-vs-environment-blocked","expected":"business non-applicability may N/A; missing environment blocks"},
    {"id":"snapshot-existing-output-safe","expected":"existing output is rejected without deletion or parent chmod"},
    {"id":"tracked-delete-staging-stable","expected":"tracked deletion snapshots and staging-only deletion keeps deliverable digest"},
    {"id":"snapshot-policy-bound","expected":"exact ignored/sensitive policy digest is reused through completion"},
    {"id":"complete-terminal","expected":"generic mutation cannot reopen COMPLETE"},
    {"id":"test-rework-invalidates","expected":"TEST_REWORK only through invalidate and review/plan become STALE"},
    {"id":"latest-attempt-controls","expected":"later failure cannot be hidden by earlier pass and max attempts applies"},
    {"id":"state-version-conflict","expected":"only one mutation with old state_version succeeds"}
  ]
}
