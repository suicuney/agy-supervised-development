{
  "skill": "agy-supervised-development",
  "version": "4.1.0-alpha.3",
  "execution_status": "SCENARIOS_DEFINED_NOT_EXECUTED",
  "cases": [
    {"id": "contract-content-bound", "expected": "same revision with changed Contract content is rejected"},
    {"id": "command-plan-only", "expected": "formal Test Plan contains only mandatory command checks or explicit no-check acceptance"},
    {"id": "recorded-command-consistency", "expected": "completion requires exact frozen argv/cwd, accepted exit code and evidence hashes"},
    {"id": "latest-attempt-controls", "expected": "latest bounded attempt determines the current check result and earlier failures remain recorded"},
    {"id": "deliverable-change-invalidates", "expected": "post-review deliverable changes invalidate review and formal evidence"},
    {"id": "environment-blocked", "expected": "missing browser/auth/dependency remains BLOCKED and cannot remove a frozen command"},
    {"id": "snapshot-policy-bound", "expected": "exact ignored/sensitive snapshot policy is reused through TEST-entry and completion"},
    {"id": "complete-terminal", "expected": "generic mutation cannot reopen COMPLETE"},
    {"id": "test-rework-invalidates", "expected": "TEST rework uses the invalidate path and stales review/plan/results"},
    {"id": "dispatch-writer-safety", "expected": "SEND_UNKNOWN or unknown prior writer cannot authorize another execution"}
  ]
}
